name: openpay-sdk CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build docker
      run: docker build . -t openpay-sdk:latest
    - name: Run Tests
      run: docker run --rm -e CLIENT_ID=mzdtln0bmtms6o3kck8f -e CLIENT_SECRET=sk_e568c42a6c384b7ab02cd47d2e407cab openpay-sdk:latest rake test
    - name: Run RuboCop
      run: openpay-sdk:latest rake rubocop
